# Copy to .env.local to enable optional features. The core app needs NONE of these.

# Enables the natural-language query box (/api/nl). Get a key at
# https://console.anthropic.com. Server-side only — never exposed to the browser.
# ANTHROPIC_API_KEY=sk-ant-...
